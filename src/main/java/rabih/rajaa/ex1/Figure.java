package rabih.rajaa.ex1;

/**
 * Interface for Figure - Composite Pattern
 * Represents a drawable figure that can be a leaf (Circle, Rectangle) or a composite (group of figures).
 */
public interface Figure {
    void draw();
}
